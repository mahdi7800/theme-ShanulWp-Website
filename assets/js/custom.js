jQuery(document).ready(function ($) {// coment reply    jQuery('.reply').on('click', function (e) {        e.preventDefault(); // جلوگیری از رفتار پیش‌فرض لینک        let el = jQuery(this);        let commentID = el.data('comment-id'); // دریافت ID کامنت        let comment_author = el.data('comment-author'); // دریافت نام نویسنده کامنت        let comment_content = el.data('main-comment-content'); // دریافت محتوای کامنت        jQuery('#comment_parent').val(commentID);        jQuery('.reply-to').text('پاسخ به : ' + comment_author);        jQuery('.main-comment-content').show().text(comment_content);    });});// document.getElementById('confirmUnsubscribe').addEventListener('click', function() {//     // انجام عملیات لغو اشتراک با AJAX////     // بستن مودال//     var modal = bootstrap.Modal.getInstance(document.getElementById('unsubscribeModal'));//     modal.hide();// });// document.addEventListener('DOMContentLoaded', function() {//     const urlParams = new URLSearchParams(window.location.search);//     if (urlParams.has('unsubscribe') && urlParams.get('unsubscribe') === '1') {//         const modal = new bootstrap.Modal(document.getElementById('unsubscribeModal'));//         modal.show();//     }// });